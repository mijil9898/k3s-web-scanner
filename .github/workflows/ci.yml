name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: [main]
  workflow_dispatch: {}

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ──────────────────────────────────────────────
  # 1. Frontend — package.json validation
  # (src/ is empty, no vite.config — skip build)
  # ──────────────────────────────────────────────
  check-frontend:
    name: Frontend — Validate package.json
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate package.json is well-formed
        run: node -e "JSON.parse(require('fs').readFileSync('apps/frontend/package.json','utf8')); console.log('✅ package.json valid')"

  # ──────────────────────────────────────────────
  # 2. Backend — TypeScript type check
  # (no package-lock.json → use npm install)
  # ──────────────────────────────────────────────
  check-backend:
    name: Backend — TypeScript Check
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/backend
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          # cache keyed on package.json (no package-lock.json in repo)
          cache: "npm"
          cache-dependency-path: apps/backend/package.json

      - name: Install dependencies
        run: npm install --ignore-scripts

      - name: TypeScript type check
        run: npx tsc --noEmit

  # ──────────────────────────────────────────────
  # 3. malware-analyzer — pytest + coverage
  # ──────────────────────────────────────────────
  test-malware-analyzer:
    name: malware-analyzer — pytest
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/malware-analyzer
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: pip-${{ runner.os }}-${{ hashFiles('apps/malware-analyzer/requirements-dev.txt') }}
          restore-keys: pip-${{ runner.os }}-

      - name: Install system dependencies
        run: |
          sudo apt-get update -y
          sudo apt-get install -y --no-install-recommends \
            libmagic1 \
            yara

      - name: Install Python dependencies
        run: pip install -r requirements-dev.txt

      - name: Run pytest with coverage
        run: |
          pytest tests/ -v --tb=short \
            --cov=. \
            --cov-report=xml:coverage.xml \
            --cov-report=term-missing

      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: apps/malware-analyzer/coverage.xml
          retention-days: 7

      - name: Write test summary
        if: always()
        run: |
          echo "## 🧪 pytest — malware-analyzer" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          pytest tests/ --tb=no -q 2>&1 | tail -5 >> $GITHUB_STEP_SUMMARY || true

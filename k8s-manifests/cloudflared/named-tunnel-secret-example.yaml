apiVersion: v1
kind: Secret
metadata:
  name: cloudflared-tunnel-credentials
  namespace: mijil
type: Opaque
stringData:
  # Thay thế giá trị bên dưới bằng toàn bộ nội dung JSON của tệp thông tin xác thực
  # được tạo bởi: `cloudflared tunnel create <NAME>` trên máy trạm của bạn.
  # KHÔNG BAO GIỜ commit thông tin xác thực thực sự vào git - hãy giữ chúng cục bộ/riêng tư.
  credentials.json: |
    {
      "placeholder": "replace-with-json-from-cloudflared-tunnel-create",
      "note": "DO NOT COMMIT REAL CREDENTIALS"
    }

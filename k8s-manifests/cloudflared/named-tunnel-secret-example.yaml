apiVersion: v1
kind: Secret
metadata:
  name: cloudflared-tunnel-credentials
  namespace: mijil
type: Opaque
stringData:
  # Replace the value below with the full JSON content of the credentials file
  # created by: `cloudflared tunnel create <NAME>` on your workstation.
  # DO NOT commit real credentials to source control — keep them local/private.
  credentials.json: |
    {
      "placeholder": "replace-with-json-from-cloudflared-tunnel-create",
      "note": "DO NOT COMMIT REAL CREDENTIALS"
    }
